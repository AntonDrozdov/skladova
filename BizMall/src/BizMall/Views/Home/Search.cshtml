@{
    int acount = ViewBag.ArticlesVM.Count;
    double tmp = acount / 3;
    int tmp2 = (int)Math.Floor(tmp);
    int cols = (acount % 3 == 0) ? (acount / 3) : (tmp2 + 1);//по три картинки в ряд
    cols = (cols > 3) ? 3 : cols;
}

<div class="col-xs-12 col-sm-9">
    <!--ЗАГОЛОВОК-->
    @if (ViewBag.PageTitle != null)
    {
        <h1 style="color:#b2b1b1">@ViewBag.PageTitle</h1>
    }

    <!--САМИ СТАТЬИ-->
    @for (int j = 0; j < acount; j++)
    {
        @await Component.InvokeAsync("ArticleTile", new { avm = ViewBag.ArticlesVM[j] })
    }
</div>

<!--ОБЛАКО ТЕГОВ-->
<div class="col-xs-12 col-sm-3">
    @await Component.InvokeAsync("TagCloud", null )
    </br>
    </br>
    </br>
    </br>
    </br>
    <hr />
    <div style="text-align:center;">
        <span style="font-size:15px; font-weight:bold; color:#808080;">Наши партнеры:</span>
        <br />
        <br />
        <a class="btn btn-info" target="_blank" href="http://www.buisnessdev.ru">BuisnessDev.ru</a>
        <hr />
    </div>
</div>

<div class="col-xs-12 ">
    @await Component.InvokeAsync("SearchPagination", new { pi = ViewBag.PagingInfo })
</div>            



